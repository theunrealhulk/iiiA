# iiiA
## Setup Anaconda 🐍 with python on linux
🚨🚨🚨 make sure `python` and `wget` are installed<br>
Verify your cpu architecture on linux run

```bash
uname -m
```
if output is 
```bash
#if output is 
# x86_64
#run this comman
wget https://repo.anaconda.com/archive/Anaconda3-2025.06-0-Linux-x86_64.sh
```
```bash
#if output is 
# aarch64
#run this comman
wget https://repo.anaconda.com/archive/Anaconda3-2025.06-0-Linux-aarch64.sh
```
follow install wizard setp by step<br>
